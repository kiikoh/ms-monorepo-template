import { {{ camelCase resourceName }}sApi } from "{{ kebabCase resourceName}}s-client";
import { ServerInferHandler } from "../../helpers/infer-handler";

export const get{{ pascalCase resourceName }}: ServerInferHandler<
  typeof {{ camelCase resourceName }}sApi.get{{ pascalCase resourceName }}
> = async ({ params: { id } }) => {
  const {{ camelCase resourceName }} = { id: "4", title: `Hello ${id}`, body: "World" };

  // simulate a db call
  await new Promise((resolve) => setTimeout(resolve, 500));

  return {
    status: 200,
    body: {{ camelCase resourceName }} ?? null,
  };
};
